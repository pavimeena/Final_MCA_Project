<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.venuerecommendation">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.MainActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.SearchMaps"
            android:label="@string/title_activity_search_maps" />
        <activity android:name=".activity.NearbyEventsActivity" />
        <activity android:name=".activity.OwnLocationTag" />
        <activity
            android:name=".activity.DragMarker"
            android:label="@string/title_activity_drag_marker" />
        <activity
            android:name=".activity.EventActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.FinalPlaceActivity" />
        <activity android:name=".activity.SelectTagActivity" />
        <activity android:name=".activity.DetailList" />
        <activity android:name=".activity.AddTagActivity" />
        <activity android:name=".activity.AddContactActivity" />
        <activity
            android:name=".activity.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".activity.MapsActivity2" />
        <activity android:name=".activity.MapsActivity3" />
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.SendSmsActivity" />
        <activity android:name=".activity.ViewContactActivity" />
        <activity android:name=".activity.ViewContactActivity2" />
        <activity android:name=".activity.ViewGroupActivity" />
        <activity android:name=".activity.ViewGroupActivity2" />
        <activity android:name=".activity.GroupActivity"  />
        <activity android:name=".activity.ProfileActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.SelectSingleTagActivity" />
        <activity android:name=".activity.AddFamilyContactActivity" />
        <activity android:name=".activity.SingleMapsActivity3" />
        <activity android:name=".activity.IpAddressActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>

</manifest>